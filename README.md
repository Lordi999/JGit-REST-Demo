# JGit-REST-Demo

Small Demo to use JGit through a REST-API

## Credentials needed!

In order to push your changes you may need to create a `credentials.properties` file in the root of the Project with the following contents:

````
github.username=
github.token=
````
